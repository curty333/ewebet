<div class="jumbotron">
  <h1>EVERY LINE.  EVERY GAME.</h1>
  <h2>real time odds for game changing events</h2>
</div>



<div class="center">
<div class="row">
  <% @listings.each do |listing| %>
  <div class="col-md-3">
    <%= image_tag listing.image.url(:medium) %><br>
    <%= listing.name %><br>
    <%= listing.description %><br>
    <%= number_to_currency(listing.price) %><br>
    <p><%= "Placed by #{listing.user.name}" %></p>
    <%= link_to 'Show', listing, class: "btn btn-link" %><br>
    
    
  </div>
  <% end %>
</div>
</div>



<p id="notice"><%= notice %></p>



<br>
<% if user_signed_in? %>
<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>


